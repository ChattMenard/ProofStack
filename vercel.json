{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        },
        {
          "key": "Access-Control-Allow-Methods",
          "value": "GET, POST, PUT, DELETE, OPTIONS"
        },
        {
          "key": "Access-Control-Allow-Headers",
          "value": "Content-Type, Authorization"
        }
      ]
    }
  ],
  "crons": [
    {
      "path": "/api/cron/weekly-digest",
      "schedule": "0 1 * * 6,0"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "database": {
    "migrations": [
      {
        "name": "Add discord_user_id to profiles",
        "sql": "ALTER TABLE profiles ADD COLUMN IF NOT EXISTS discord_user_id TEXT;"
      },
      {
        "name": "Create index for discord_user_id",
        "sql": "CREATE INDEX IF NOT EXISTS idx_profiles_discord_user_id ON profiles(discord_user_id);"
      },
      {
        "name": "Comment on discord_user_id column",
        "sql": "COMMENT ON COLUMN profiles.discord_user_id IS 'Discord user ID for role sync and community integration';"
      }
    ]
  }
}